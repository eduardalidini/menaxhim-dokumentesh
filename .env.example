# Example environment configuration (commit-safe)
# NOTE: If your password contains special URL characters (e.g. + & ? !), it must be URL-encoded in DATABASE_URL.
# Example: + => %2B, & => %26, ? => %3F, ! => %21

# Async SQLAlchemy style
# DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DBNAME

# Supabase pooler example (replace placeholders)
DATABASE_URL=postgresql+asyncpg://postgres.edsanoefivqeelicuqdf:YOUR_URL_ENCODED_PASSWORD@aws-1-eu-west-1.pooler.supabase.com:6543/postgres

JWT_SECRET=change_me
JWT_EXPIRES_MINUTES=60

# Optional seed admin on startup (first run)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=Admin123!

# Google Drive (Service Account)
# Preferred for deployment: paste the *entire contents* of the downloaded service-account JSON here.
# Most hosting providers have a "Secrets"/"Environment Variables" UI where you can paste it.
GOOGLE_CREDENTIALS_JSON=

# Folder ID where uploads will be stored
DRIVE_FOLDER_ID=your_drive_folder_id
